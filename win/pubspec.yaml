name: vpn_win
description: Windows Flutter shell for the commercial VPN platform.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  client_sdk:
    path: ../client_sdk

flutter:
  uses-material-design: true
